[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "p3dwm"
version = "0.1.0"
description = "Predictive 3D workspace world model for robotic manipulation"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]